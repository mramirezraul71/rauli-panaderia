{
  "name": "rauli-erp-panaderia",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  }
}
